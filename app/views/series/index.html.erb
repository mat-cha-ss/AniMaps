<h1>Anime</h1>

<table>
  <thead>
    <tr>
      <th>Series</th>
      <th>Locations</th>
      <th>Events</th>
    </tr>
  </thead>

  <tbody>
    <% @series.each do |series| %>
      <tr>
        <td><%= series.official_title("ja") %></td>
        <td>
        <%= link_to(
          series.animes.joins(:locations).distinct.count("locations.id"),
          anime_series_locations_path(series)
        ) %>
        </td>
        <td>
        <%= link_to(
          series.animes.joins(:events).distinct.count("events.id"),
          anime_series_events_path(series)
        ) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>